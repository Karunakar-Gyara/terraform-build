steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
      
        echo "${_BUCKET_ONE}"
        chmod +X variable.sh
        bash variable.sh

options:
  logging: CLOUD_LOGGING_ONLY
